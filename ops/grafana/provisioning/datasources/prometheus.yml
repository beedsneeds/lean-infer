apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      # Match the scrape interval so Grafana does not ask for a finer step than exists.
      timeInterval: 1s
