# Fields dcgm-exporter should publish. Deliberately minimal.
#
# Everything here is an NVML device field, which is why this file exists at all:
# the image default (dcp-metrics-included.csv) turns on the DCGM_FI_PROF_* profiling
# counters, which need the profiling engine, SYS_ADMIN on the container, and a DCGM
# build that matches the driver. None of that is worth it until you know what you
# want to look at.
#
# Format: DCGM field, prometheus metric type, help string
#
# Next ones worth adding, in rough order of usefulness for this engine:
#   DCGM_FI_DEV_SM_CLOCK          - separates power throttling from thermal throttling
#   DCGM_FI_DEV_GPU_TEMP          - ditto
#   DCGM_FI_PROF_SM_ACTIVE        - real occupancy, unlike GPU_UTIL (needs SYS_ADMIN)
#   DCGM_FI_PROF_DRAM_ACTIVE      - memory bandwidth; the compute-vs-bandwidth answer
#                                   for decode (needs SYS_ADMIN)

DCGM_FI_DEV_FB_USED,        gauge, Framebuffer memory used (MiB).
DCGM_FI_DEV_GPU_UTIL,       gauge, Fraction of time any kernel was resident (%). Coarse - reads near 100 during decode regardless of how well the SMs are fed.
DCGM_FI_DEV_POWER_USAGE,    gauge, Power draw (W). The L4 caps at 72W, so this pinning is the first sign of clock throttling.
